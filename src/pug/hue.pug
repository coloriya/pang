extends _base

block content
	+PaletteList(hue_page.palettes)
	div.pagination
		h3 Hue #{hue.hue_start}&deg; palettes
		each p in hue.pages
			| !{app.nl}
			if p == page
				a.current= p.number
			else
				a(href=p.relativeURL)= p.number


